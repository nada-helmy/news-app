import 'package:flutter/material.dart';

class SetingsHome extends StatelessWidget {
  const SetingsHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.cyan,
    );
  }
}
